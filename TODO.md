# TODO
- [ ] Inspect existing content/work MDX(s) and app/about/page.tsx to determine how “work content” should be represented.
- [ ] Add additional work experience entries to app/about/page.tsx (separate section(s) for work experience).
- [ ] Ensure content is consistent with timeline and does not break styling/layout.
- [ ] (If needed) run `npm test` / `npm run build` to confirm compilation.

